import { Chemistry, FlashFilled, Bot } from '@vicons/carbon'

export const teamTypeIconConfig = {
  backend: Chemistry,
  frontend: FlashFilled,
  bot: Bot,
}

export const teamTypeColorConfig = {
  backend: '#63E2B7',
  frontend: '#F2E355',
  bot: '#55B0F2',
}

export const teamTypesConfig = {
  frontend: { label: 'Frontend 前端 ', value: 'frontend' },
  backend: { label: 'Backend 後端', value: 'backend' },
  bot: { label: 'Bot 開發', value: 'bot' },
}
